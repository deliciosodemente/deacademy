export function renderNotFound(){
  return `
  <section class="section container">
    <h2>Página no encontrada</h2>
    <p class="lead">La ruta que has intentado abrir no existe.</p>
    <a class="btn btn-primary" href="#/">Volver al inicio</a>
  </section>`;
}

