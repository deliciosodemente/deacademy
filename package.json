@@ .. @@
   "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "test": "vitest",
+    "test:ui": "vitest --ui",
+    "test:run": "vitest run",
+    "build": "vite build",
+    "preview": "vite preview",
+    "dev": "vite"
   },